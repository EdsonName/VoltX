document.addEventListener('DOMContentLoaded',()=>{const link=document.querySelector('#link_url'),form=document.querySelector('.ad-create-form');if(link){link.type='text';link.placeholder='narrativas.site';const hint=document.createElement('small');hint.textContent='Você pode informar somente o domínio. HTTPS será tratado automaticamente.';link.after(hint)}if(form&&!form.querySelector('[name="duracao_segundos"]')){const field=document.createElement('div');field.className='form-group exposure-field';field.innerHTML='<label for="duracao_segundos">Tempo de exposição</label><div><input id="duracao_segundos" name="duracao_segundos" type="range" min="5" max="20" value="10" step="1"><output>10 segundos</output></div><small>O anúncio permanecerá visível de 5 a 20 segundos antes da próxima campanha.</small>';const linkGroup=link.closest('.form-group');linkGroup.after(field);const range=field.querySelector('input'),output=field.querySelector('output');range.addEventListener('input',()=>output.textContent=range.value+' segundos')}});
