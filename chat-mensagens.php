<?php
require_once __DIR__.'/includes/auth.php';require_once __DIR__.'/includes/functions.php';header('Content-Type: application/json; charset=utf-8');
if(!usuario_autenticado()){http_response_code(401);echo json_encode(['erro'=>'Faça login novamente.']);exit;}$usuario_id=(int)$_SESSION['usuario_id'];$conversa_id=(int)($_REQUEST['conversa_id']??0);$conversa=pegar_conversa_autorizada($conversa_id,$usuario_id);if(!$conversa){http_response_code(403);echo json_encode(['erro'=>'Conversa não autorizada.']);exit;}
if($_SERVER['REQUEST_METHOD']==='POST'){$mensagem=trim($_POST['mensagem']??'');if(!validar_csrf($_POST['csrf_token']??'')){http_response_code(419);echo json_encode(['erro'=>'A sessão expirou.']);exit;}if($mensagem===''||mb_strlen($mensagem)>2000){http_response_code(422);echo json_encode(['erro'=>'A mensagem deve ter entre 1 e 2.000 caracteres.']);exit;}$stmt=$mysqli->prepare('INSERT INTO mensagens(conversa_id,remetente_id,mensagem) VALUES(?,?,?)');$stmt->bind_param('iis',$conversa_id,$usuario_id,$mensagem);$stmt->execute();$stmt=$mysqli->prepare('UPDATE conversas SET atualizado_em=NOW() WHERE id=?');$stmt->bind_param('i',$conversa_id);$stmt->execute();}
$stmt=$mysqli->prepare('UPDATE mensagens SET lida=1 WHERE conversa_id=? AND remetente_id<>?');$stmt->bind_param('ii',$conversa_id,$usuario_id);$stmt->execute();$stmt=$mysqli->prepare('SELECT m.id,m.remetente_id,m.mensagem,DATE_FORMAT(m.criado_em,"%d/%m %H:%") criado_em,u.nome remetente_nome FROM mensagens m JOIN usuarios u ON u.id=m.remetente_id WHERE m.conversa_id=? ORDER BY m.id ASC LIMIT 300');$stmt->bind_param('i',$conversa_id);$stmt->execute();echo json_encode(['mensagens'=>$stmt->get_result()->fetch_all(MYSQLI_ASSOC)],JSON_UNESCAPED_UNICODE);
